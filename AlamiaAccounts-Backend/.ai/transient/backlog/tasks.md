# Backend Backlog Tasks

- [ ] Run backend test suite (`phpunit` / `php artisan test`).
- [ ] Verify `php artisan ledger:setup` generates chart of accounts correctly.
- [ ] Create Dockerfile for Laravel backend service.
