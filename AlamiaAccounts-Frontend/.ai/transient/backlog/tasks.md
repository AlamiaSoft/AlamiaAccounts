# Frontend Backlog Tasks

- [ ] Run `npm run build` in `AlamiaAccounts-Frontend` to verify zero TypeScript errors.
- [ ] Ensure `.env.local` is set with valid `NEXT_PUBLIC_API_URL`.
- [ ] Create Dockerfile for frontend production deployment.
